size_t static write_callback_0 (void *buffer, size_t size, size_t nmemb, void *ptr) {
    strcpy(responses[0],(const char*)buffer);
    return strlen(responses[0]);
}
size_t static write_callback_1 (void *buffer, size_t size, size_t nmemb, void *ptr) {
    strcpy(responses[1],(const char*)buffer);
    return strlen(responses[1]);
}
size_t static write_callback_2 (void *buffer, size_t size, size_t nmemb, void *ptr) {
    strcpy(responses[2],(const char*)buffer);
    return strlen(responses[2]);
}
size_t static write_callback_3 (void *buffer, size_t size, size_t nmemb, void *ptr) {
    strcpy(responses[3],(const char*)buffer);
    return strlen(responses[3]);
}
size_t static write_callback_4 (void *buffer, size_t size, size_t nmemb, void *ptr) {
    strcpy(responses[4],(const char*)buffer);
    return strlen(responses[4]);
}
size_t static write_callback_5 (void *buffer, size_t size, size_t nmemb, void *ptr) {
    strcpy(responses[5],(const char*)buffer);
    return strlen(responses[5]);
}
size_t static write_callback_6 (void *buffer, size_t size, size_t nmemb, void *ptr) {
    strcpy(responses[6],(const char*)buffer);
    return strlen(responses[6]);
}
size_t static write_callback_7 (void *buffer, size_t size, size_t nmemb, void *ptr) {
    strcpy(responses[7],(const char*)buffer);
    return strlen(responses[7]);
}
size_t static write_callback_8 (void *buffer, size_t size, size_t nmemb, void *ptr) {
    strcpy(responses[8],(const char*)buffer);
    return strlen(responses[8]);
}
size_t static write_callback_9 (void *buffer, size_t size, size_t nmemb, void *ptr) {
    strcpy(responses[9],(const char*)buffer);
    return strlen(responses[9]);
}
size_t static write_callback_10 (void *buffer, size_t size, size_t nmemb, void *ptr) {
    strcpy(responses[10],(const char*)buffer);
    return strlen(responses[10]);
}
size_t static write_callback_11 (void *buffer, size_t size, size_t nmemb, void *ptr) {
    strcpy(responses[11],(const char*)buffer);
    return strlen(responses[11]);
}
size_t static write_callback_12 (void *buffer, size_t size, size_t nmemb, void *ptr) {
    strcpy(responses[12],(const char*)buffer);
    return strlen(responses[12]);
}
size_t static write_callback_13 (void *buffer, size_t size, size_t nmemb, void *ptr) {
    strcpy(responses[13],(const char*)buffer);
    return strlen(responses[13]);
}
size_t static write_callback_14 (void *buffer, size_t size, size_t nmemb, void *ptr) {
    strcpy(responses[14],(const char*)buffer);
    return strlen(responses[14]);
}
size_t static write_callback_15 (void *buffer, size_t size, size_t nmemb, void *ptr) {
    strcpy(responses[15],(const char*)buffer);
    return strlen(responses[15]);
}
size_t static write_callback_16 (void *buffer, size_t size, size_t nmemb, void *ptr) {
    strcpy(responses[16],(const char*)buffer);
    return strlen(responses[16]);
}
size_t static write_callback_17 (void *buffer, size_t size, size_t nmemb, void *ptr) {
    strcpy(responses[17],(const char*)buffer);
    return strlen(responses[17]);
}
size_t static write_callback_18 (void *buffer, size_t size, size_t nmemb, void *ptr) {
    strcpy(responses[18],(const char*)buffer);
    return strlen(responses[18]);
}
size_t static write_callback_19 (void *buffer, size_t size, size_t nmemb, void *ptr) {
    strcpy(responses[19],(const char*)buffer);
    return strlen(responses[19]);
}
size_t static write_callback_20 (void *buffer, size_t size, size_t nmemb, void *ptr) {
    strcpy(responses[20],(const char*)buffer);
    return strlen(responses[20]);
}
size_t static write_callback_21 (void *buffer, size_t size, size_t nmemb, void *ptr) {
    strcpy(responses[21],(const char*)buffer);
    return strlen(responses[21]);
}
size_t static write_callback_22 (void *buffer, size_t size, size_t nmemb, void *ptr) {
    strcpy(responses[22],(const char*)buffer);
    return strlen(responses[22]);
}
size_t static write_callback_23 (void *buffer, size_t size, size_t nmemb, void *ptr) {
    strcpy(responses[23],(const char*)buffer);
    return strlen(responses[23]);
}
size_t static write_callback_24 (void *buffer, size_t size, size_t nmemb, void *ptr) {
    strcpy(responses[24],(const char*)buffer);
    return strlen(responses[24]);
}
size_t static write_callback_25 (void *buffer, size_t size, size_t nmemb, void *ptr) {
    strcpy(responses[25],(const char*)buffer);
    return strlen(responses[25]);
}
size_t static write_callback_26 (void *buffer, size_t size, size_t nmemb, void *ptr) {
    strcpy(responses[26],(const char*)buffer);
    return strlen(responses[26]);
}
size_t static write_callback_27 (void *buffer, size_t size, size_t nmemb, void *ptr) {
    strcpy(responses[27],(const char*)buffer);
    return strlen(responses[27]);
}
size_t static write_callback_28 (void *buffer, size_t size, size_t nmemb, void *ptr) {
    strcpy(responses[28],(const char*)buffer);
    return strlen(responses[28]);
}
size_t static write_callback_29 (void *buffer, size_t size, size_t nmemb, void *ptr) {
    strcpy(responses[29],(const char*)buffer);
    return strlen(responses[29]);
}
size_t static write_callback_30 (void *buffer, size_t size, size_t nmemb, void *ptr) {
    strcpy(responses[30],(const char*)buffer);
    return strlen(responses[30]);
}
size_t static write_callback_31 (void *buffer, size_t size, size_t nmemb, void *ptr) {
    strcpy(responses[31],(const char*)buffer);
    return strlen(responses[31]);
}
